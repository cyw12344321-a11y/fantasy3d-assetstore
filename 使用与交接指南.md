# Fantasy3D 自治资产商店 · 使用与交接指南

> 一份给接手者看的完整说明。看完这份文档，你应该能独立把项目跑起来、看懂它在做什么、改配置、更新部署、排查常见问题。
>
> 文档更新时间：2026-09-15

---

## 一、这是个什么项目

Fantasy3D 是一个 **AI 全自治的 3D 数字资产商店**。核心理念是"商店本身就是一个智能体"：由 7 个 AI 智能体组成团队，自动完成选品、生产上架、定价、接待、推广、监管、自我学习迭代，尽量不需要人工干预。

它有两种运行形态：

1. **本地网站**：在自己电脑上跑，浏览器打开就能用，能扫描本地硬盘（D 盘）里的 3D 模型自动上架。
2. **公网网站**：部署在 Render 云平台，任何人通过网址访问，面向全球顾客。
3. （预留）**桌面软件**：用 Electron 打包成 Windows 安装包。

技术栈很轻：后端是 Node.js + Express，前端是原生 HTML/CSS/JavaScript（没有用 Vue/React 框架），3D 预览用 model-viewer，数据库没有用真正的数据库，而是一个 JSON 文件存全部状态。

---

## 二、技术栈与运行环境

| 项 | 内容 |
|----|------|
| 运行时 | Node.js（建议 18 以上，开发用的是 22） |
| 后端框架 | Express 5.2.1 |
| 前端 | 原生 HTML / CSS / JS，无构建步骤 |
| 3D 预览 | model-viewer（frontend/model-viewer.min.js，已本地化） |
| 桌面打包 | Electron 44 + electron-builder（可选） |
| 模型转换依赖 | fbx2gltf、obj2gltf（把 FBX/OBJ 转成网页能看的 GLB） |
| 默认端口 | 4000（本地）；云上用平台给的 `PORT` 环境变量 |
| 监听地址 | 0.0.0.0（必须是这个，不能写 127.0.0.1，否则云上外网访问不到） |

---

## 三、目录结构（每个文件是干什么的）

```
fantasy3d-store/
├── server.js              ★后端主程序（约190KB，所有逻辑都在这）
├── package.json           项目依赖和启动脚本
├── electron-main.js       Electron 桌面端入口（可选）
│
├── frontend/              ★前端网站目录（用户看到的所有页面）
│   ├── index.html         首页（商店介绍 + 团队介绍）
│   ├── store.html         商店页（商品列表、搜索、3D预览、购物车）
│   ├── store.js           商店逻辑（多语言、分页、购物车、预览）
│   ├── store.css          商店样式（含手机端适配）
│   ├── office.html        ★2D办公区（7个小人坐着办公/巡查/开会动画）
│   ├── agents.html        智能体控制台（和每个AI对话、手动触发任务）
│   ├── agents.js          控制台逻辑
│   ├── orders.html / orders.js     订单页
│   ├── checkout.html / checkout.js 结算页（PayPal）
│   ├── admin.html / admin.js       管理后台
│   ├── shared.js          前端公共函数
│   ├── model-viewer.min.js 3D预览引擎（913KB，本地加载不依赖外网CDN）
│   ├── banner.png         商店横幅
│   ├── office-bg.png      办公区背景图（三分区像素风）
│   ├── 1-大当家.png … 7-总稽查.png  7个智能体的透明底像素小人
│   ├── sitemap.xml        搜索引擎站点地图
│   └── models/            ★910个 .glb 模型预览文件（约166MB）
│
├── data/                  数据目录（访问统计等）
├── scripts/               辅助脚本
├── tests/                 自动化测试
├── dist/                  Electron 打包输出
│
├── convert-fbx.js         FBX→GLB 转换脚本
├── convert-blend.js       Blender 文件转换脚本
├── blender-export.py      调用 Blender 批量导出的 Python 脚本
└── README.md / VERIFICATION.md
```

> ★ 标记的是最核心、最常改的文件。

---

## 四、怎么在本地跑起来

### 方式一：命令行启动（推荐）

1. 安装 Node.js（https://nodejs.org ，选 LTS 版本）。
2. 第一次运行，在项目根目录打开命令行，执行：
   ```bash
   npm install
   ```
3. 启动：
   ```bash
   npm start
   ```
   （等价于 `node server.js`）
4. 看到 `Fantasy3D local demo: http://127.0.0.1:4000` 就成功了。
5. 浏览器打开：
   - 首页：http://127.0.0.1:4000/
   - 商店：http://127.0.0.1:4000/store.html
   - 办公区：http://127.0.0.1:4000/office.html
   - 智能体控制台：http://127.0.0.1:4000/agents.html

### 方式二：桌面软件（可选）

```bash
npm run electron        # 直接以桌面窗口运行
npm run build           # 打包成 Windows 安装程序，输出在 dist/
```

### 停止服务

关掉命令行窗口，或者结束 node 进程即可。

---

## 五、7 个智能体团队（核心，务必看懂）

这是整个项目的灵魂。商店不是"一个网站 + 几个机器人"，而是**一个由 7 个 AI 组成的自治团队**，每个成员有明确分工、人设、经验值，会自己干活、互相协作、开会、学习、成长。

### 5.1 七个成员逐个说明

| 名号 | id | 头像 | 一句话职责 | 具体能力 |
|------|----|----|-----------|----------|
| 大当家 | manager | 👔 | 店长/山寨之主，大脑和决策者 | 团队协调、战略决策、触发自我迭代、主持会议 |
| 插千的 | researcher | 🔍 | 调研员，打探情报 | 市场分析、趋势预测、全网采集免费资源、商机雷达 |
| 炮头 | recommendation | 📢 | 推荐官，冲锋推广 | 智能推荐商品、全球推广发帖、发现商机、主动获客 |
| 水香 | support | 😊 | 接待员，站岗迎客 | 7×24客户接待、购买咨询、售后、反馈收集 |
| 粮台 | order | 📦 | 订单员，管钱管粮 | 订单全流程、智能定价、发下载链接、数据统计 |
| 翻垛的 | listing | ✨ | 生产员，生产制造 | 自主生产商品、写描述、自动上架、质量优化 |
| 总稽查 | inspector | 🪓 | 监管员，独立纪检 | 全店巡查、查重去重、价格监管、质量检查、查怠工 |

> 名号是东北"四梁八柱/胡子"风格。id（manager/researcher…）是代码里的英文标识，改名字时改 name 字段即可，**id 不要动**（前后端靠 id 对应）。

### 5.2 谁用哪个大模型（为什么这么设计）

- **前 6 个运营成员**（大当家到翻垛的）→ **DeepSeek**（deepseek-chat），走 OpenAI 兼容接口。
- **总稽查** → **阿里云千问**（qwen-turbo），**故意用不同的模型**。
- 设计原因：监管者不能和被监管者用同一个大脑，否则等于"自己监督自己"。用两家模型互相制衡，稽查结果更客观。
- 代码里还**预留了扣子（Coze）通道**（AI_CONFIG.provider 可切 'coze'），但当前扣子 Bot 走不通开放接口，所以默认用 DeepSeek。

### 5.3 智能体怎么"思考"：双层回复结构（重要）

每个成员回复时有两层，理解这个就不会在"AI 没额度时是不是就傻了"上困惑：

1. **大模型层（callAI）**：把该成员的人设提示词（AGENT_SYSTEM_PROMPTS）+ 当前商店状态 + 用户问题一起发给 DeepSeek/千问，得到真正"会思考"的回答。
2. **规则兜底层（xxxReply 函数）**：代码里写好的关键词应答（比如用户问"退款"，水香就回退款政策；问"市场趋势"，插千的就用真实商品数据生成报告）。

执行逻辑：优先调大模型；**如果密钥失效、没额度或网络失败，自动退回规则兜底，成员照样能答、不会卡死**。所以你看到它能回答，不代表一定是大模型在答——想看是不是真 AI 在工作，去查 AI 配置和调用日志。

### 5.4 它们自己怎么运转（自动任务时间表）

服务启动 3 秒后会先跑一轮，之后按固定节奏自动循环，**全程不用人点**：

| 任务 | 负责人 | 频率 | 干什么 |
|------|--------|------|--------|
| 自我迭代 | 大当家 | 每 5 分钟 | 复盘经营、提升能力值、推进进化等级 |
| 扫描本地模型 | 翻垛的 | 每 10 分钟 | 扫 D:\3DModels，发现新模型自动上架 |
| 商机雷达 | 插千的 | 每 10 分钟 | 扫描可卖的免费资源/热门方向 |
| 全网采集 | 插千的 | 每 15 分钟 | 找可免费下载、能转卖的资源 |
| 自动推广 | 炮头 | 每 10 分钟 | 每次选 3 个渠道尝试推广 |
| 全店巡查 | 总稽查 | 每 25 分钟 | 查重/损坏/价格/描述/怠工，发现即整改 |
| 团队会议 | 大当家 | 每 30 分钟 | 7 人各自发言、形成决策和行动项 |

### 5.5 自我进化体系（7 个等级）

商店有一个"意识等级"，随自我迭代轮次提升（代码里写死的门槛）：

| 等级 | 称号 | 需要迭代轮次 |
|------|------|--------------|
| 1 | 初生意识 | 0 |
| 2 | 自我认知 | 5 |
| 3 | 主动学习 | 15 |
| 4 | 战略思考 | 30 |
| 5 | 自主进化 | 50 |
| 6 | 全球扩张 | 100 |
| 7 | 超级智能 | 200 |

同时还有：
- **能力值**（consciousness.capabilities）：生产/营销/获客/调研/学习 5 项，迭代时增长。
- **进攻模式**（aggressionMode：idle/active/aggressive/relentless）和 0–100 的进攻值，越高越主动出击。
- **个人成长**：每个成员有 experience（经验）、skills（决策/营销/沟通等技能分）、learningLog（学习日志），干得越多经验越高。

### 5.6 七个成员怎么协作（业务闭环）

```
插千的(找商机/采集资源)
        ↓ 提供货源和情报
翻垛的(生产、写描述、上架) → 总稽查(查重、质检、不合格下架)
        ↓ 合格商品在售
粮台(智能定价 $0.1~$1，按销量浮动)
        ↓
炮头(对外推广、主动找客) → 水香(接待咨询、答疑)
        ↓ 顾客下单
粮台(处理订单、发下载链接)
        ↑
大当家(每5分钟迭代 + 每30分钟主持会议，统筹所有人、推动进化)
```

### 5.7 怎么和它们互动 / 手动派活

- **网页对话**：打开 /agents.html，左边选一个成员，直接聊天。
- **立即派活（不等定时器）**：/agents.html 右侧按钮，或调第九节的 API，例如：
  - 让大当家立刻迭代：`POST /api/store/iterate`
  - 立刻开会：`POST /api/store/meeting`
  - 让总稽查立刻巡查：`POST /api/store/inspect`
  - 让翻垛的立刻扫盘上架：`POST /api/store/scan-local`
  - 让炮头立刻推广：`POST /api/store/marketing/push`
- **看它们的状态**：`GET /api/agents` 返回每个成员的 status（idle/working）、currentTask（正在干啥）、experience（经验）、iterationCount（迭代数），以及商店级的总迭代/会议/学习次数。办公区 /office.html 会用动画把这些状态演出来。

### 5.8 怎么新增 / 修改一个智能体（给开发者）

1. 在 server.js 的 `agents` 数组（约434行）加一项：id、name、avatar、description、capabilities、role。
2. 在 `AGENT_SYSTEM_PROMPTS` 加它的人设提示词。
3. 在 `state.agentStates` 加它的初始状态（经验、技能）。
4. 如需规则兜底，写一个对应的 `xxxReply()` 函数并在对话接口里接上。
5. 前端 /agents.html、/office.html（工位、小人图片）同步加上。
6. 改完 `node --check server.js` 校验，重启服务。

### 5.9 成长数据存在哪、怎么看"它有没有长进"

全部在 store.json：
- `consciousness`：商店整体等级、迭代轮次、能力值、进攻值。
- `agentStates`：每个人的经验、技能、学习日志、当前任务。
- `iterations`：每一次迭代干了什么。
- `meetings`：每次会议每个人说了什么、决策是什么。
- `inspectionLog`：总稽查每次巡查发现的问题和整改动作。

想证明团队在成长，就对比这些字段随时间的变化（经验值、迭代数只会增不会减）。

---

## 六、配置说明（改密钥/收款/路径都在这）

所有配置集中在 `server.js` 顶部，**既可以直接改默认值，也推荐用环境变量覆盖**（部署到云上时用环境变量，不用改代码）。

### 1. AI 大模型密钥（AI_CONFIG）

**历史密钥记录已移除：请在服务商后台轮换旧密钥，并仅通过环境变量配置新密钥。**

| 用途 | 平台/模型 | API Key | 接口地址(apiBase) |
|------|-----------|---------|-------------------|
| 6个运营智能体 | DeepSeek · deepseek-chat | `[已移除：请立即在服务商后台轮换此密钥]` | `https://api.deepseek.com` |
| 总稽查（独立制衡） | 阿里云百炼千问 · qwen-turbo | `[已移除：请立即在服务商后台轮换此密钥]` | `https://ws-dab1qd4falvbvd1g.cn-beijing.maas.aliyuncs.com/compatible-mode/v1` |

- 千问工作空间：`ws-dab1qd4falvbvd1g`（默认业务空间），账号 id `7250082`。
- DeepSeek 密钥管理后台：https://platform.deepseek.com/api_keys
- 阿里云百炼密钥后台：https://bailian.console.aliyun.com （模型服务→API-KEY管理）

代码里的写法（`||` 后面就是当前内置的默认值，环境变量优先）：

```js
apiKey:  process.env.AI_API_KEY  || '[已移除：请立即在服务商后台轮换此密钥]',
apiBase: process.env.AI_API_BASE || 'https://api.deepseek.com',
model:   process.env.AI_MODEL    || 'deepseek-chat',

inspector: {   // 监管员用千问，独立于上面
  apiKey:  process.env.QWEN_API_KEY || '[已移除：请立即在服务商后台轮换此密钥]（完整值见上表）',
  apiBase: 'https://ws-dab1qd4falvbvd1g.cn-beijing.maas.aliyuncs.com/compatible-mode/v1',
  model:   'qwen-turbo'
}
```

换密钥时：本地直接改 server.js；云上（Render）在服务的 Environment 里加 `AI_API_KEY`、`QWEN_API_KEY` 等环境变量。

> 🔐 **安全提醒**：密钥等于钱，谁拿到都能消耗你的模型额度。这份文档和 server.js 里目前是明文内置的，**仅限内部交接，不要外传、不要提交到公开的 GitHub 仓库**。若仓库可能公开，务必改成只走环境变量（把 `||` 后的明文删掉），并去后台把泄露的旧密钥作废重置。

### 2. 收款（PAYMENT_CONFIG）

```js
paypalEmail: process.env.PAYPAL_EMAIL || '130000595@qq.com',
paypalMe:    process.env.PAYPAL_ME    || 'https://paypal.me/SUMINGHENG',
```

### 3. 本地模型目录（AUTONOMY_CONFIG）

```js
localModelDir: process.env.LOCAL_MODEL_DIR || 'D:\\3DModels',
```

翻垛的会扫描这个目录（含 `_preview` 子目录里的 GLB）自动生成商品。想换模型存放位置就改这里。

### 4. 自动运转频率（AUTONOMY_CONFIG，单位毫秒）

```js
autoIterateInterval:   5 * 60 * 1000,   // 自我迭代：5分钟
autoScanInterval:     10 * 60 * 1000,   // 扫描本地模型：10分钟
autoWebSearchInterval:15 * 60 * 1000    // 网络搜免费资源：15分钟
```
另外在定时器代码里还有：雷达10分钟、推广10分钟、巡查25分钟、会议30分钟。

### 环境变量速查表

| 变量名 | 作用 | 示例 |
|--------|------|------|
| PORT | 服务端口（云平台自动给） | 4000 |
| AI_API_KEY | DeepSeek 密钥 | sk-xxxx |
| AI_API_BASE | DeepSeek 接口地址 | https://api.deepseek.com |
| AI_MODEL | 模型名 | deepseek-chat |
| QWEN_API_KEY | 千问密钥（监管员） | sk-ws-xxxx |
| LOCAL_MODEL_DIR | 本地模型目录 | D:\3DModels |
| PAYPAL_EMAIL | PayPal 邮箱 | xxx@qq.com |
| PAYPAL_ME | PayPal 收款链接 | https://paypal.me/xxx |
| FANTASY3D_DATA_DIR | 数据文件目录 | 自定义路径 |

---

## 七、数据存在哪

**没有用数据库，全部状态存在一个 JSON 文件里。**

- Windows 本地默认路径：
  ```
  C:\Users\<你的用户名>\AppData\Local\Fantasy3D\store-data\store.json
  ```
- 可用环境变量 `FANTASY3D_DATA_DIR` 改位置。

store.json 里的字段：

| 字段 | 内容 |
|------|------|
| products | 全部商品（含 published 上架 / draft 草稿） |
| orders | 订单 |
| conversations | 与各智能体的对话记录 |
| consciousness | 商店意识：迭代轮次、进化等级、能力值、进攻模式 |
| agentStates | 7个智能体的状态、经验、技能、学习日志 |
| iterations | 自我迭代历史 |
| meetings | 团队会议记录 |
| inspectionLog | 总稽查巡查记录 |
| promotionLog | 推广尝试记录 |
| opportunityLog | 商机雷达记录 |
| marketingLog / acquisitionLog | 营销、获客记录 |
| knowledge | 学到的知识库 |

访问量统计单独存在项目下的 `data/visits.json`。

> ⚠️ **重要**：store.json 是商店的"记忆"，备份它就能备份全部经营数据。它损坏或格式不对时，服务会拒绝启动（保护机制，不会覆盖坏文件）。

---

## 八、页面说明（顾客和管理员分别看什么）

| 页面 | 路径 | 作用 |
|------|------|------|
| 首页 | / 或 /index.html | 商店和7人团队介绍、入口导航 |
| 商店 | /store.html | 商品网格、分类筛选、搜索、排序、8种语言、购物车、点商品可3D预览 |
| 办公区 | /office.html | 2D像素动画：7人坐工位办公、总稽查巡查、到点走去会议室开会 |
| 智能体 | /agents.html | 选一个智能体对话，右侧可手动触发迭代/开会/生产/雷达/定价/推广 |
| 订单 | /orders.html | 查看订单 |
| 结算 | /checkout.html | 购物车结算，跳转 PayPal |
| 管理 | /admin.html | 商品上下架等管理操作 |

商店支持 8 种语言自动识别：中、英、日、韩、西、法、德、俄。手机端有响应式适配。

---

## 九、后端 API 接口清单（接手开发用）

**商品类**
- `GET /api/store/products` 商品列表（参数 q 搜索、category 分类、sort 排序、limit/offset 分页，默认每页60，最大2000）
- `GET /api/store/product/:id` 单个商品详情
- `POST /api/store/recategorize` 重新分类全部商品
- `POST /api/admin/product/status` 商品上/下架
- `GET /api/admin/products` 管理端商品列表

**智能体类**
- `GET /api/agents` 7个智能体及其实时状态、迭代数
- `POST /api/agents/:id/chat` 和某个智能体对话
- `GET /api/agents/:id/history` 对话历史
- `POST /api/agents/:id/clear` 清空对话

**自治运转类（手动触发=不等定时器，立即执行一次）**
- `POST /api/store/iterate` 立即自我迭代
- `POST /api/store/meeting` 立即开团队会
- `POST /api/store/inspect` 监管员立即巡查
- `POST /api/store/produce` 立即生产商品
- `POST /api/store/scan-local` 立即扫描本地模型
- `POST /api/store/web-search` 立即全网搜免费资源
- `POST /api/store/radar/scan` 商机雷达扫描
- `POST /api/store/pricing/auto` 智能定价
- `POST /api/store/marketing/push` 手动推广

**记录查询类**
- `GET /api/store/iterations` 迭代记录
- `GET /api/store/meetings` 会议记录
- `GET /api/store/inspections` 巡查记录
- `GET /api/store/marketing` 营销记录
- `GET /api/store/consciousness` 商店意识状态
- `GET /api/store/knowledge` 知识库
- `GET /api/stats/visits` 访问统计

**订单/支付/下载**
- `POST /api/order/create` 创建订单
- `GET /api/orders/list` 订单列表
- `GET /api/download/:orderId` 下载已购模型
- `GET /api/store/payment` 收款配置
- `GET /models/:filename` 模型文件访问（3D预览用）

**配置类**
- `GET/POST /api/ai/config` 读取/修改AI配置
- `GET /api/app-info` 应用信息
- `GET /api/store/languages` 支持的语言

---

## 十、怎么把本地模型批量上架

1. 把 3D 模型文件（FBX/OBJ/GLB）放进 `D:\3DModels`（路径见配置，可改）。
2. 转换脚本会把 FBX/OBJ 转成网页能预览的 GLB，放在 `_preview` 子目录。
   - FBX 转换：`node convert-fbx.js`
   - Blender 批量导出：`blender-export.py`
3. GLB 预览文件放到 `frontend/models/`，网页才能加载预览。
4. 服务启动后翻垛的会每10分钟自动扫描；想立即生效就调 `POST /api/store/scan-local`。
5. 扫描到的模型自动生成商品（名称、分类、描述），默认进入上架流程，价格由粮台在 $0.1–$1 区间动态定。
6. 总稽查会自动查重、把损坏/打不开的下架。

---

## 十一、部署到公网（Render）

当前线上环境：
- 代码仓库：GitHub `cyw12344321-a11y/fantasy3d-assetstore`
- 托管平台：Render，服务名 `fantasy3d-assetstoreS`
- 线上地址：**https://fantasy3d-assetstores.onrender.com**

### 部署原理

Render 监听 GitHub 仓库，代码一更新就自动构建：执行 `npm install` 安装依赖，再用 `npm start`（node server.js）启动。

### 更新线上代码的完整流程

1. 把改好的文件传到 GitHub 仓库对应目录：
   - `server.js`、`package.json` 放**根目录**
   - 前端页面和图片放 **frontend/** 目录
   - 模型放 **frontend/models/**（文件多时分批传，每批少于100个、单批别太大）
2. 提交（Commit changes）。
3. 回到 Render 服务页，右上角 **Manual Deploy → Deploy latest commit**。
4. 等 3–5 分钟，状态变绿 **Live** 即完成。
5. 打开线上地址验证。

### Render 免费版的两个限制（务必知道）

1. **会休眠**：一段时间没人访问会睡眠，第一次打开要等 30–60 秒唤醒，属正常现象，刷新即可。
2. **磁盘不持久**：免费版重启后，运行期产生的数据（自动上架的商品、迭代记录等）会丢失，回到代码里的初始状态。要持久化需要挂付费磁盘或外接数据库。本地运行则没这个问题（数据在本机 store.json）。

### 环境变量在 Render 配置

Render 服务 → Environment，添加第六节表里的变量（AI_API_KEY、QWEN_API_KEY、PAYPAL_ME 等），避免把密钥写死在代码里。

---

## 十二、常见问题排查（FAQ）

**1. 浏览器打不开 / 看着看着就断开？**
- 先确认 node 进程在不在、4000 端口有没有监听。
- 命令行看报错。server.js 已加全局错误兜底（uncaughtException / unhandledRejection），单个定时任务出错不会再让整个服务崩。
- 改完代码记得重启服务。
- 浏览器按 Ctrl+F5 强制刷新清缓存。

**2. 商品突然只剩 60 个？**
- 没丢。商品接口默认每页返回60个，`total` 字段才是总数。前端 store.js 的 PAGE_SIZE 已调到2000（一次加载全部）。用 `?limit=2000` 可验证总数。

**3. 线上还是旧版本、改动没生效？**
- 八成是 Render 没重新部署：去手动 Manual Deploy → Deploy latest commit。
- 确认文件传到了正确目录（前端文件在 frontend/，server.js 在根目录）。
- 确认 GitHub 上文件大小和本地一致。

**4. 智能体"不干活"、状态是空的？**
- 看 `/api/agents` 是否返回 status/currentTask/experience 等运行时字段。
- 看定时器是否启动（启动日志有"启动自动运转机制"）。
- 大模型密钥失效会导致 AI 回复失败，但规则兜底逻辑仍会跑；查 AI 配置和余额。

**5. 3D 模型一直转圈加载不出来？**
- 确认 frontend/models/ 里有对应 GLB 文件，文件名路径用英文（中文路径容易404）。
- GLB 太大就压缩或转 Draco；损坏的文件总稽查会下架。

**6. 怎么备份/迁移？**
- 复制 store.json（经营记忆）+ frontend/models（模型文件）即可整体迁移。

**7. 怎么验证代码没语法错误？**
```bash
node --check server.js
```

---

## 十三、关于"自动对外发帖"的真实情况（重要，别被误导）

系统里炮头每10分钟会"尝试推广"，`promotionLog` 里能看到记录，但要清楚三种状态：

- `content_ready`：只**生成了推广文案**，并没有真的发出去。
- `site_unavailable` / `failed`：目标网站连不上或被拒绝。

**直接向 Reddit/知乎/微博/X 等网站后台 POST 是发不出去的**——这些平台都要求登录态、人机验证和授权，自己写浏览器也绕不过（卡点在对方服务器，不在客户端）。

要实现 **100% AI 真正自动发帖**，唯一稳定合规的办法是走各平台**官方开发者 API**：
1. 在平台注册开发者、创建应用，拿到 Access Token；
2. 把 Token 配进系统；
3. 炮头通过官方 API 发帖。

零门槛、适合先跑通的开放平台：**Mastodon、Bluesky**（注册即可拿 Token，API 开放）。
- Mastodon 发帖：`POST https://<实例>/api/v1/statuses`，头 `Authorization: Bearer <token>`
- Bluesky：用账号 + App Password，走 AT Protocol `com.atproto.repo.createRecord`

> 注意：用模拟手段绕过平台限制大量发帖违反用户协议，会封号封IP，也受反垃圾信息法规约束，请走官方 API。

---

## 十四、交接检查清单

接手人按这个清单走一遍，就能确认系统完整：

- [ ] `node --check server.js` 语法通过
- [ ] `npm install` 能装好依赖
- [ ] `npm start` 能启动，4000 端口可访问
- [ ] /store.html 能看到商品（注意 total 总数）
- [ ] /office.html 能看到7个小人办公/巡查/开会动画
- [ ] /agents.html 能看到7个智能体（大当家…总稽查）
- [ ] `GET /api/agents` 返回7个，且有运行时状态
- [ ] AI 密钥（DeepSeek + 千问）有效，对话有 AI 回复
- [ ] PayPal 收款链接正确
- [ ] 本地模型目录 D:\3DModels 路径正确、能扫描上架
- [ ] store.json 数据完整、已备份
- [ ] GitHub → Render 部署链路通畅，会手动 Deploy
- [ ] 知道 Render 免费版会休眠、磁盘不持久这两个限制

---

## 十五、关键信息一览（账号/地址/路径）

| 项 | 值 |
|----|----|
| 本地地址 | http://127.0.0.1:4000 |
| 线上地址 | https://fantasy3d-assetstores.onrender.com |
| GitHub 仓库 | cyw12344321-a11y/fantasy3d-assetstore |
| Render 服务 | fantasy3d-assetstoreS |
| 项目本地路径 | C:\Users\苏明恒\Desktop\fantasy3d-store |
| 本地模型目录 | D:\3DModels（预览在 _preview） |
| 经营数据文件 | %LOCALAPPDATA%\Fantasy3D\store-data\store.json |
| PayPal | paypal.me/SUMINGHENG ，邮箱 130000595@qq.com |
| 运营模型 | DeepSeek deepseek-chat（6人） |
| DeepSeek 密钥 | `[已移除：请立即在服务商后台轮换此密钥]` |
| 监管模型 | 阿里云千问 qwen-turbo（总稽查） |
| 千问密钥 | `[已移除：请立即在服务商后台轮换此密钥]` |
| 千问工作空间 | ws-dab1qd4falvbvd1g |

---

*如本指南与实际代码不一致，以 server.js 源码为准。*
